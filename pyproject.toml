[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "myllm-tiny"
version = "0.1.0"
description = "A small from-scratch decoder-only Transformer language model"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["myllm_tiny*"]

